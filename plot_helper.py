# -*- coding: utf-8 -*-
"""
Created on Sat Aug 17 23:40:46 2019

@author: SMART
"""

import numpy as np
import pandas as pd
import seaborn as sns
import matplotlib.pyplot as plt


def plot_corr(corr,diag_flag=False,figure_size=(11,9),title=None):
    
    """
    Plotting a diagonal correlation matrix
    ======================================
    input 
    corr - correlation matrix generated by pandas
    diag_flag - (bool) if True makes diagonal matrix instead of full
    figure_size - (tuple) figure ssize
    title - (str) fig title 
    """
    sns.set(style="white")
    if diag_flag:
        # Generate a mask for the upper triangle
        mask = np.zeros_like(corr, dtype=np.bool)
        mask[np.triu_indices_from(mask)] = True
    else: 
        mask=None
    
    # Set up the matplotlib figure
    f, ax = plt.subplots(figsize=figure_size)
    
    # Generate a custom diverging colormap
    cmap = sns.diverging_palette(220, 10, as_cmap=True)
    
    # Draw the heatmap with the mask and correct aspect ratio
    sns.heatmap(corr, mask=mask, cmap=cmap, vmax=1, center=0,
                square=True, linewidths=.5, cbar_kws={"shrink": .5})
    if title != None :
        plt.title(title,fontsize = 18)

        
    return f
